# Install a PyTorch build for your hardware first:
# https://pytorch.org/get-started/locally/
#
# FFmpeg is also required on PATH for MP3/OGG/M4A output, speed changes,
# and stitched non-WAV files. FFmpeg is a system dependency, not a pip package.

voxcpm>=2.0.2,<3
gradio>=6,<7
librosa>=0.11,<1
numpy>=2,<3
soundfile>=0.13,<1
